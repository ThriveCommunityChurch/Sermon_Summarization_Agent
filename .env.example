# OpenAI API Key (required for Whisper transcription and GPT-4o-mini summarization)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Whisper model to use (default: small.en)
# Options: tiny, tiny.en, base, base.en, small, small.en, medium, medium.en, large
# Larger models are more accurate but slower. GPU highly recommended for medium/large.
WHISPER_MODEL=small.en

# Optional: Force CPU mode for Whisper (default: false)
# Set to "true" to disable GPU acceleration even if CUDA is available
# GPU (CUDA) is automatically detected and used if available for much faster transcription
WHISPER_FORCE_CPU=false

# Optional: Directory to search for sermon files if no file path is provided
SERMON_AUDIO_DIR=C:\Users\Videos

# Optional: Specific sermon file path (can also be provided via CLI argument)
# SERMON_FILE_PATH=C:\path\to\sermon.mp4
